#include <stdio.h>

/**
 * main - prints all single-digit numbers of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int z;

	for (z = 0; z < 10; z++)
		putchar(z);

	putchar('\n');
	return (0);
}
